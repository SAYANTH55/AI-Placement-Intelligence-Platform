fix: resolve black screen bug, add dashboard sub-routes, mobile nav, and premium UI upgrades

Bug Fixes:
- Fix UploadResume.jsx navigating to non-existent /job-input route (black screen on upload)
- Add sub-routes in Dashboard.jsx for all 4 sidebar links (analysis, skills, score, recommendations)
- Remove silent wildcard redirect, replace with proper 404 NotFound page
- Wire LandingPage navbar links to react-router (were broken href anchors with no destination)
- Add CORS middleware to FastAPI backend for localhost:5173 and localhost:3000

New Components:
- Add NotFound.jsx: professional 404 page with back-to-home action
- Add ScoreRing.jsx: animated SVG donut/ring score visualization (replaces flat ProgressBar)
- Add MobileNav.jsx: slide-in hamburger overlay for dashboard mobile navigation

UI/UX Improvements:
- Upgrade InsightCards with ScoreRing and new Placement Probability card (4th card)
- Improve Sidebar user card with colored avatar circle showing user initials
- Add Auto-Fill button on Login page for one-click demo credential entry
- Each dashboard sidebar sub-page shows proper empty state when no analysis done
- Enrich mockData.js with salary ranges, match percentages, company names, and more recommendations

Code Quality:
- Fix all missing lucide-react imports in Dashboard.jsx (Target, TrendingUp, Briefcase)
- Dashboard refactored into clean AnalysisPage, SkillsPage, ScorePage, RecommendationsPage functions
- UploadBox, InsightCards, mockData updated to handle enriched data object format
