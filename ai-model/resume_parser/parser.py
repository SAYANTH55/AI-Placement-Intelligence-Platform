import spacy

def parse_resume(file_path):
    # Placeholder for NLP parsing logic
    nlp = spacy.load("en_core_web_sm")
    # text = extract_text(file_path)
    # doc = nlp(text)
    return {"skills": ["Python", "JavaScript"], "experience": "2 years"}
